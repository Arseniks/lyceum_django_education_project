[tool.black]
line-length = 79
skip-string-normalization = true
target-version = ['py310']
include = '\.pyi?$'

[tool.isort]
profile = "google"

[tool.flake8]
per-file-ignores = [
    '*/migrations/:E501',
]
max-complexity = 10

